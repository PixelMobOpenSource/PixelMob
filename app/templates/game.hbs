<div class="span2"><img {{bind-attr src=cover.cover.url}} class="largeImage" style="height:200px;"></img></div>
<div class="span6">{{description}}</div>
<legend>{{name}}</legend>
<ul class="thumbnails">
{{#each video in videos}}
	{{partial thumbnail}}
{{/each}}
</ul>
