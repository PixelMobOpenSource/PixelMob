<legend>{{name}}</legend>
<div class="col-sm-10">
	<div class="row">
	    {{#each video in videos}}
		    {{video-thumbnail video=video currentUser=controllers.currentUser}}
	    {{/each}}
	</div>
</div>
<div class="col-sm-2 tagDescription">
	{{{description}}}
</div>
